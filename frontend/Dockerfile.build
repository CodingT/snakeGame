FROM node:20-alpine AS builder

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Build the application
RUN npm run build

# Export static files
RUN npm run export || echo "Export not configured, using build output"

# The output will be in /app/out or /app/.next
